package com.althaus.dev.cookIes.theme

import androidx.compose.ui.graphics.Color

// =======================================
// Tema Claro
// =======================================

// Fondo y superficies
val LightBackground = Color(0xFFEEE6C0) // Fondo principal
val LightSurface = Color(0xFFFFFFFF) // Superficies elevadas

// Colores primarios y secundarios
val LightPrimary = Color(0xFF6D4C41) // Color primario (también usado para textos)
val LightPrimaryVariant = Color(0xFF8D6E63) // Variante del primario
val LightSecondary = Color(0xFFD7C6A0) // Color secundario
val LightSecondaryVariant = Color(0xFFBCA68D) // Variante del secundario

// Colores de error
val LightError = Color(0xFFBF374F) // Color de error
val LightErrorVariant = Color(0xFFB66073) // Variante del error
val LightErrorBackground = Color(0xFFFFE5E9) // Fondo para mensajes de error

// Colores de texto y elementos destacados
val LightOnPrimary = Color(0xFFFFFFFF) // Texto sobre primario
val LightOnSecondary = Color(0xFF000000) // Texto sobre secundario
val LightOnBackground = LightPrimary // Texto sobre fondo (usando `LightPrimary` como texto)
val LightOnSurface = Color(0xFF000000) // Texto sobre superficies
val LightOnError = Color(0xFFFFFFFF) // Texto sobre color de error

// =======================================
// Tema Oscuro
// =======================================

// Fondo y superficies
val DarkBackground = Color(0xFF121212) // Fondo principal
val DarkSurface = Color(0xFF1F1F1F) // Superficies elevadas

// Colores primarios y secundarios
val DarkPrimary = Color(0xFFBB86FC) // Color primario
val DarkPrimaryVariant = Color(0xFF3700B3) // Variante del primario
val DarkSecondary = Color(0xFF03DAC6) // Color secundario
val DarkSecondaryVariant = Color(0xFF018786) // Variante del secundario

// Colores de error
val DarkError = Color(0xFFCF6679) // Color de error
val DarkErrorVariant = Color(0xFFB00020) // Variante del error
val DarkErrorBackground = Color(0xFF3A1D1F) // Fondo para mensajes de error

// Colores de texto y elementos destacados
val DarkOnPrimary = Color(0xFF000000) // Texto sobre primario
val DarkOnSecondary = Color(0xFF000000) // Texto sobre secundario
val DarkOnBackground = Color(0xFFE0E0E0) // Texto sobre fondo
val DarkOnSurface = Color(0xFFE0E0E0) // Texto sobre superficies
val DarkOnError = Color(0xFFFFFFFF) // Texto sobre color de error
