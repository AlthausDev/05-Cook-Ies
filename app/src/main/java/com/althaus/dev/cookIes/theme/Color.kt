package com.althaus.dev.cookIes.theme

import androidx.compose.ui.graphics.Color

// Colores principales del tema
val PrimaryLight = Color(0xFFEEE6C0)
val PrimaryDark = Color(0xFF6D4C41)

// Colores secundarios
val SecondaryLight = Color(0xFFD7C6A0)

// Color de error más suave
val ErrorLight = Color(0xFFBF374F) // Ajusta el tono para que sea menos agresivo
//val ErrorLight = Color(0xFFB66073) // Ajusta el tono para que sea menos agresivo
val ErrorBackground = Color(0xFFFFE5E9) // Fondo suave para mensajes de error


// Colores principales del tema oscuro
val DarkBackground = Color(0xFF121212) // Fondo principal
val DarkSurface = Color(0xFF1F1F1F) // Fondo para superficies elevadas
val DarkPrimary = Color(0xFFBB86FC) // Color principal (morado suave)
val DarkSecondary = Color(0xFF03DAC6) // Color secundario (verde agua)
val DarkError = Color(0xFFCF6679) // Color de error (rojo suave)

// Colores para texto y elementos destacados
val DarkOnBackground = Color(0xFFE0E0E0) // Texto principal
val DarkOnSurface = Color(0xFFCCCCCC) // Texto secundario
val DarkOnPrimary = Color(0xFF000000) // Contraste en botones principales
val DarkOnSecondary = Color(0xFF000000) // Contraste en botones secundarios
val DarkOnError = Color(0xFF000000) // Contraste en mensajes de error