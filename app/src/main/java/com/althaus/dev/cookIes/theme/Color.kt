package com.althaus.dev.cookIes.theme

import androidx.compose.ui.graphics.Color

// Colores principales del tema
val PrimaryLight = Color(0xFFEEE6C0)       // Claro para el tema principal (ParchmentLight)
val PrimaryDark = Color(0xFFD7C6A0)        // Oscuro para el tema principal (ParchmentDark)
val TextPrimary = Color(0xFF6D4C41)        // Marrón oscuro para el texto principal

// Colores secundarios
val SecondaryGray = Color(0xFF393939)
val SecondaryBlack = Color(0xFF121212)
val AccentGreen = Color(0xFF49dd63)        // Verde para acentos o confirmaciones

// Colores de botones
val ButtonBackground = Color(0xFF111111)
val ButtonShape = Color(0xFF3e3e3e)

// Colores de campos de selección o estado
val FieldSelected = Color(0xFF7c7c7c)
val FieldUnselected = Color(0xFF4a4a4a)

// Colores adicionales de fondo y texto
val BackgroundDark = Color(0xFF121212)     // Fondo oscuro, si es necesario
val BackgroundLight = Color(0xFFEEEEEE)    // Fondo claro opcional
