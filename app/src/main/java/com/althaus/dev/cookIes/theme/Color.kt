package com.althaus.dev.cookIes.theme

import androidx.compose.ui.graphics.Color

// =======================================
// Tema Claro
// =======================================

/** Fondo principal del tema claro. */
val LightBackground = Color(0xFFEEE6C0)

/** Superficies elevadas en el tema claro. */
val LightSurface = Color(0xFFFFFFFF)

/** Color principal del tema claro. */
val LightPrimary = Color(0xFF6D4C41)

/** Variante del color principal en el tema claro. */
val LightPrimaryVariant = Color(0xFF8D6E63)

/** Color secundario del tema claro. */
val LightSecondary = Color(0xFFD7C6A0)

/** Variante del color secundario en el tema claro. */
val LightSecondaryVariant = Color(0xFFBCA68D)

/** Color de error en el tema claro. */
val LightError = Color(0xFFBF374F)

/** Variante del color de error en el tema claro. */
val LightErrorVariant = Color(0xFFB66073)

/** Fondo para mensajes de error en el tema claro. */
val LightErrorBackground = Color(0xFFFFE5E9)

/** Texto sobre el color principal en el tema claro. */
val LightOnPrimary = Color(0xFFFFFFFF)

/** Texto sobre el color secundario en el tema claro. */
val LightOnSecondary = Color(0xFF000000)

/** Texto sobre el fondo principal en el tema claro. */
val LightOnBackground = LightPrimary

/** Texto sobre superficies elevadas en el tema claro. */
val LightOnSurface = Color(0xFF000000)

/** Texto sobre el color de error en el tema claro. */
val LightOnError = Color(0xFFFFFFFF)

// =======================================
// Tema Oscuro
// =======================================

/** Fondo principal del tema oscuro. */
val DarkBackground = Color(0xFF121212)

/** Superficies elevadas en el tema oscuro. */
val DarkSurface = Color(0xFF1F1F1F)

/** Color principal del tema oscuro. */
val DarkPrimary = Color(0xFFFFFFFF)

/** Variante del color principal en el tema oscuro. */
val DarkPrimaryVariant = Color(0xFF3700B3)

/** Color secundario del tema oscuro. */
val DarkSecondary = Color(0xFF849595)

/** Variante del color secundario en el tema oscuro. */
val DarkSecondaryVariant = Color(0xFF849595)

/** Texto genérico en el tema oscuro. */
val DarkText = Color(0xFFFFFFFF)

/** Color de error en el tema oscuro. */
val DarkError = Color(0xFFCF6679)

/** Variante del color de error en el tema oscuro. */
val DarkErrorVariant = Color(0xFFB00020)

/** Fondo para mensajes de error en el tema oscuro. */
val DarkErrorBackground = Color(0xFF3A1D1F)

/** Texto sobre el color principal en el tema oscuro. */
val DarkOnPrimary = Color(0xFF000000)

/** Texto sobre el color secundario en el tema oscuro. */
val DarkOnSecondary = Color(0xFF000000)

/** Texto sobre el fondo principal en el tema oscuro. */
val DarkOnBackground = Color(0xFFE0E0E0)

/** Texto sobre superficies elevadas en el tema oscuro. */
val DarkOnSurface = Color(0xFFE0E0E0)

/** Texto sobre el color de error en el tema oscuro. */
val DarkOnError = Color(0xFFFFFFFF)
